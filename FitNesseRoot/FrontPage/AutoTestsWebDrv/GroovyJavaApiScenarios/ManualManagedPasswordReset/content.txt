!*> Test description
1. ForceReset command does to not support manually managed accounts.
2. For the manually managed accounts ForceResetManual command is used.  It returns Password and PasswordID.
3. ManualPasswordReset still provides the final step for manually managed accounts.
*!

!include -c <AutoTestsWebDrv.PlatformsTesting.WindowsDesktop

!define expectedBtnStatus {Check Password Button is Disabled}
!define ForceResetResult {You must use ForceResetManual command}
!define ManualManagedAccount {${prefix}Manul}

!include -c <AutoTestsWebDrv.GroovyJavaApiScenarios.ListCommands.ScenarioLibrary
!include -c <AutoTestsWebDrv.GroovyJavaApiScenarios.ScenarioLibrary

!|script                                                                                                                                                                                      |
|Add System                               |SystemName=${SystemName};NetworkAddress=${SystemAddress};FunctionalAccount=${FunctAcct};FuncAcctCred=${FunctAcctPwd};PlatformName=${SystemPlatform}|
|Add Account with options                 |${SystemName}                     |                        |${ManualManagedAccount}           |                     |AutoFlag=M                    |
|ForceReset for manual managed account    |${ManualManagedAccount}           |from                    |${SystemName}                     |results in           |${ForceResetResult}           |
|Force Reset manual password              |${ManualManagedAccount}           |from                    |${SystemName}                                                                          |
|Confirm Manual Password reset with status|fail                              |and result is           |Success                                                                                |
|Force Reset manual password              |${ManualManagedAccount}           |from                    |${SystemName}                                                                          |
|Confirm Manual Password reset with status|success                           |and result is           |Success                                                                                |

!*> CleanUp
!|script                    |
|Remove System|${SystemName}|
*!