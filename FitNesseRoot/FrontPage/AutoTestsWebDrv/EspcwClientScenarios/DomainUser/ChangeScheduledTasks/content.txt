!include -c <AutoTestsWebDrv.ScenarioLibrary
!include -c <AutoTestsWebDrv.CliScenarios.ScenarioLibrary
!include -c <AutoTestsWebDrv.EspcwClientScenarios.DomainUser.ScenarioLibrary
!include -c <AutoTestsWebDrv.EspcwClientScenarios.LocalUser.ChangeScheduledTasks.TestSpecific

!define TaskName {TestTaskOne}

!|script                                                                                                                                                         |
|Prepare environment on           |${SystemAddress}                                                                                                              |
|Change Account Password on System|${ManagedAccountPwdNew}                                                                                                       |
|Create Scheduled Task            |${TaskName}      |which is run under|${ManagedAccount}|and run under password is|${ManagedAccountPwdNew}|on|${DMSystemAddress}|
|Check Checkbox                   |changetasks      |for user          |DEFAULT                                                                                  |
|Reset Password                   |${ManagedAccount}|                  |${ResetPasswordSuccessful}                                                               |
|Run Scheduled Task               |${TaskName}      |on                |${DMSystemAddress}                                                                       |
|Check Scheduled Task Status      |Running          |on                |${DMSystemAddress}                                                                       |
|Stop Scheduled Task              |${TaskName}      |on                |${DMSystemAddress}                                                                       |
|Uncheck Checkbox                 |changetasks      |for user          |DEFAULT                                                                                  |
|Reset Password                   |${ManagedAccount}|                  |${ResetPasswordSuccessful}                                                               |
|Run Scheduled Task               |${TaskName}      |on                |${DMSystemAddress}                                                                       |
|Check Scheduled Task Status      |Could.not.start  |on                |${DMSystemAddress}                                                                       |
|Restore environment on           |${SystemAddress}                                                                                                              |
