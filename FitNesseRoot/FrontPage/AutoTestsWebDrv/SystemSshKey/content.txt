!note Managed System settings
!define SystemName {${prefix}keyhole}
!define SystemPlatform {Linux}
!define SystemAddress {10.30.46.27}
!define SystemTimeout {60}
!define SystemDescription {Test system for SSHKey command}
!define rootAcct {root}
!define rootAcctPwd {=1qwerty}
!define FunctAcct {sshtpam}
!define KeyFile {${ProjDir}\Scripts\keys\id_dsa.pub}
!define ManagedAccount {${prefix}keykeeper}

!define CliSysAdmin {${prefix}syscliadmin}
!define addSystemMethod {Add System with specific key}
!define addPsmAccountMethod {Add system and PSM account with specific key}
!define successLogon {psmKeyAuthSuccess.png,psmKeyAuthSuccess2.png}
!define failedLogon {psmKeyRefused.png}

!contents -g -p -f -h
