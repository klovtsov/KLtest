!* Text description
 It's impossible to create user with same Certificate Thumbprint. It should be unique.
Error should arise and test checks such error.
*!
!define UserName {${prefix}Test}
!define ThumbPrint {D33BE9039AAB1047F20943B9FF98D5014A593C2E}
!define CreateTemplateResult {Cannot insert duplicate key row in object 'dbo.Credentials'}

!include -c <FrontPage.AutomatedTesting.GroovyJavaApiScenarios.ScenarioLibrary
!include -c <AutomatedTesting.PowerShellNetApiScenarios.ScenarioLibrary
!include -c <PropagateOptionsFromTemplate.ScenarioLibrary

!4 Create example user, make user template and create a new user from template
!|script                                                                                                   |
|Add User          |${TestUserName}|with options|-PrimaryAuthType Certificate -CertThumbprint ${ThumbPrint}|
|Make User Template|${TemplateName}|from        |${TestUserName}   |with result  |${CreateTemplateResult}  |

!*> Fake steps for TearDown
!|script                 |
|Add User|${TemplateName}|
|Add User|${newUserName} |
*!