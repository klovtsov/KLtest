!define ManagedAccount {${prefix}psAcnt}
!define count {1}
!define type {User}
!define name {ParAdmin}
!include -c <PowerShellNetApiScenarios.ScenarioLibrary

!|script                                                                                                                                                                                                                                                 |
|Add system                                                                                                                                                                                                                                              |
|Set Permissions        |Reviewer                                                                                                                 |on system|${SystemName}|for user    |${name}                                                          |
|Add account            |${ManagedAccount}                                                                                                        |on system|${SystemName}|with options|-ReviewCount ${count} -ReviewerType ${type} -ReviewerName ${name}|
|List Accounts by filter|-systemName ${SystemName} -AccountName ${ManagedAccount} -propsToList accountname,reviewCount,reviewerType,reviewerName|contains |${ManagedAccount}.*${count}.*${type}.*${name}                                               |
|Delete System          |${SystemName}                                                                                                                                                                                                                   |
