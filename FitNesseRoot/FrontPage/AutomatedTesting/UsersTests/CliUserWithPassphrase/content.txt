!* Test description
Check WEB CLI user with passphrase.
*!

!define UserName {${prefix}_Passphrase}
!define Password {Password2}
!define Passphrase {Check one two}
!define CLIcommand {ListUsers --UserName ${UserName}}

!include -c <AutomatedTesting.UsersTests.UserTypes.ScenarioLibrary

!|script                                                                                              |
|Add Web Cli user               |${UserName}                                                          |
|Change Pwd                     |${UserName}    ||Password1  ||${Password}                            |
|Add Passphrase And Download Key|${UserName}    ||${Password}||${Passphrase}                          |
|Run Cli command with Passphrase|"${Passphrase}"||${UserName}||${TPAMHost}||${CLIcommand}||${UserName}|

!*> Cleanup
!|script                                                      |
|Delete User|${CLIBatch}||--UserName ${UserName}||successfully|
|Remove Keys|${UserName}*                                     |
*!